# /data — pre-staged datasets

| file | rows | purpose |
|---|---|---|
| `sample.csv` | 500 | synthetic, generated by `scripts/gen_sample_csv.py`; intentionally has right-skewed `price`/`income`, left-skewed `balance`, ~25% missing `notes`, an outlier-heavy `tx_amount`, a near-constant `flag_v1`, and two correlated `pca1/pca2`. Used by the EDA test suite. |
| `replay_run_history.json` | — | (added in Phase 4) W&B run history for the training-monitor agent fallback. |
